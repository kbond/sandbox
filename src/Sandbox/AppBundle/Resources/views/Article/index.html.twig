{% extends '::base.html.twig' %}

{% block title %}Article list{% endblock %}

{% block body %}
    <style type="text/css">
        th, td {
            vertical-align: middle!important;
        }
    </style>

    <table class="table table-striped table-hover table-bordered">
        <thead>
        <tr>
            <th>Title</th>
            <th>Date</th>
            <th>Actions</th>
        </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td>{{ entity.title }}</td>
                <td>{% if entity.date %}{{ entity.date|date('Y-m-d') }}{% endif %}</td>
                <td>
                    <a class="btn btn-small" href="{{ path('article_edit', { 'id': entity.id }) }}">Edit</a>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <p>
        <a class="btn btn-primary" href="{{ path('article_new') }}">Create a new entry</a>
    </p>
{% endblock %}


