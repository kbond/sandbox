{% extends '::base.html.twig' %}

{% block title %}Media Manager{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <link href="{{ asset('bundles/zenstruckmedia/css/twitter.css') }}" rel="stylesheet" />
{% endblock %}

{% block body %}
    <div id="zenstruck-media">
        <div class="btn-toolbar">
            <div class="btn-group">
                {% if manager.path %}
                    <a class="btn btn-small" href="{{ path('zenstruck_media_list', { 'path': manager.ancestry[0:manager.ancestry|length-1]|join('/') }) }}"><i class="icon-arrow-left"></i></a>
                {% else %}
                    <span class="btn btn-small disabled"><i class="icon-arrow-left"></i></span>
                {% endif %}
            </div>

            <div class="btn-text">
                /
                {% if manager.path %}
                    <a href="{{ path('zenstruck_media_list') }}"><em>root</em></a>
                {% endif %}
                {% set prev_path = [] %}
                {% for dir in manager.ancestry %}
                    {% set prev_path = prev_path|merge([dir]) %}
                    {% if not loop.last %}
                        <a href="{{ path('zenstruck_media_list', { 'path': prev_path|join('/') }) }}">{{ dir }}</a> /
                    {% else %}
                        {{ dir }}
                    {% endif %}
                {% endfor %}
            </div>

            <div class="btn-group">
                <a class="btn btn-small" data-toggle="modal" href="#zenstruck-media-mkdir"><i class="icon-folder-open"></i> New</a>
                <a class="btn btn-small" data-toggle="modal" href="#zenstruck-media-upload"><i class="icon-upload"></i> Upload</a>
            </div>
        </div>
        <hr />

        {% include 'ZenstruckMediaBundle:Twitter:_table.html.twig' with { 'manager': manager } %}

        <div id="zenstruck-media-mkdir" class="modal hide fade">
            <div class="modal-header">
                <button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
                <h3>Make Directory</h3>
            </div>
            <form method="post" action="{{ path('zenstruck_media_mkdir', { 'path': manager.path }) }}" class="form-horizontal">
                <div class="modal-body">
                    <div class="control-group">
                        <label class="control-label" for="zenstruck-media-new-folder-name">Directory Name</label>
                        <div class="controls">
                            <input type="text" name="dir_name" id="zenstruck-media-new-folder-name" autofocus="autofocus">
                        </div>
                    </div>
                </div>
                <div class="modal-footer">
                    <a href="#" class="btn" data-dismiss="modal">Close</a>
                    <input type="submit" class="btn btn-primary" value="Create" />
                </div>
            </form>
        </div>
        <div id="zenstruck-media-upload" class="modal hide fade">
            <div class="modal-header">
                <button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
                <h3>Upload Files</h3>
            </div>
            <form method="post" action="{{ path('zenstruck_media_upload', { 'path': manager.path }) }}" class="form-horizontal" enctype="multipart/form-data">
                <div class="modal-body">
                    <div class="control-group">
                        <label class="control-label" for="zenstruck-media-upload-files">Select a File</label>
                        <div class="controls">
                            <input type="file" name="file" id="zenstruck-media-upload-files">
                        </div>
                    </div>
                </div>
                <div class="modal-footer">
                    <a href="#" class="btn" data-dismiss="modal">Close</a>
                    <input type="submit" class="btn btn-primary" value="Upload" />
                </div>
            </form>
        </div>
    </div>
{% endblock %}